{{/* SPDX-FileCopyrightText: The RamenDR authors */}}
{{/* SPDX-License-Identifier: Apache-2.0 */}}
{{define "conditions" -}}
<dl class="validation">
    {{- range .}}
    <dt>{{.Type}}</dt>
    <dd>
        <span class="state">{{icon .State}}</span>
        {{- if .Description}}
            <p class="description">{{.Description}}</p>
        {{- end}}
        {{- if .Reason}}
            <p class="reason">{{.Reason}}</p>
        {{- end}}
        {{- if .Suggestion}}
            <p class="suggestion">{{- if .URL}}<a href="{{.URL}}">{{.Suggestion}}</a>{{- else}}{{.Suggestion}}{{- end}}</p>
        {{- end}}
    </dd>
    {{- end}}
</dl>
{{- end}}
